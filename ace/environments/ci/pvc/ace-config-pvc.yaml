apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ace-config-pvc
spec:
  storageClassName: ibmc-file-gold-gid
  accessModes:
  - ReadWriteMany
  capacity:
    storage: 20Gi
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem